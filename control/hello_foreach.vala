void main() {
	string[] mobils = { "Honda", "Suzuki", "Ford", "Toyota", "Daihatsu" };

	stdout.printf("Nama-nama mobil: \n\n");	
	foreach (string mobil in mobils) {
		stdout.printf("%s\n", mobil);
	}
}
