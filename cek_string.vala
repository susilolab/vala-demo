public class CekString: Object
{
	public CekString() {
	}

	public bool is_numeric(string str) {
		bool result = true;
		for(int i=0; i<str.length; i++) {
			int.parse(str[i]) == ;
		}
	}

	static int main() {
		string nama = "vita";
		for(int i=0; i<nama.length; i++) {
			stdout.printf("%c", nama[i]);
		}
		return 0;
	}
}
